# Prometheus scrape configuration for go-api-template
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "go-api-template"
    metrics_path: /metrics
    static_configs:
      - targets: ["app:8080"]
